242.✅Valid Anagram
Example 1:
Input: s = "anagram", t = "nagaram"
Output: true
Example 2:
Input: s = "rat", t = "car"
Output: false
✅Approch:
1.Find The Length
2.Intialse Hashmaps
3.Scan Each character
4.check and increase the count value
5.Return true if all matches
📌
 class Solution {
    public boolean isAnagram(String s, String t) {
        //find length
        if(s.length()!=t.length()){
            return false;
        }
        //Hashmaps for character and its count
        HashMap<Character, Integer> map= new HashMap<>();
        //scan each character in s 
        //check and increase the count
        for(char ch:s.toCharArray()){
            map.put(ch,map.getOrDefault(ch,0)+1);
        }
        //scan each character in t
        //check and decrease the count
        for(char ch :t.toCharArray()){
            if(!map.containsKey(ch)){
                return false;
            }
            map.put(ch,map.get(ch)-1);
            if (map.get(ch) < 0) {
                return false;
            }

        }
        return true;
        
    }
}