49.✅Group Anagrams
Example 1:
Input: strs = ["eat","tea","tan","ate","nat","bat"]
Output: [["bat"],["nat","tan"],["ate","eat","tea"]]
✅Approch:
1.HashMap String List Of Strings
2.String Before Sort Called Values
3.Convert it into List of char then sort it into array
4.String after sorted array called keys
5.Mapping:
	map.put(key,new ArrayList<>());
6.If the key is already present add a new string to the same key
	map.get(key).add(new string);
7.Return all the values before sort
📌
class Solution {
    public List<List<String>> groupAnagrams(String[] strs) {
        HashMap<String, List<String>> map = new HashMap<>();

        for (String s : strs) {
            // Sort the string to form the key
            char[] ch = s.toCharArray();
            Arrays.sort(ch);
            String key = new String(ch);

            // Add the original string to the map
            map.putIfAbsent(key, new ArrayList<>());
            map.get(key).add(s);
        }

        // Return all group of anagrams
        return new ArrayList<>(map.values());
    }
}
⚡Note:
map.putIfAbsent(key,new ArrayList<>());
	It does not overwrites
	map = {"aet" : ["eat", "tea"]}
map.put(key,new ArrayList<>());
	It overwrites
	map = {"aet" : ["tea"]}
