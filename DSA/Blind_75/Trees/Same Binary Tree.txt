✅Same Binary Tree
⚡Approch:
1.TreeNode Class:
2.Solution Class:
	Check two trees are equal to null return true
	If One of them equals to null return false
	If both trees values are not equal return false
	Find Boolean left and right of both root.left and root.right
	Return left and right
3.Main method:
	Build 2 TreeNodes
📌

class Main {
    static class TreeNode{
        int val;
        TreeNode left;
        TreeNode right;
        TreeNode(int val){
            this.val=val;
            this.left=null;
            this.right=null;
        }
    }
    static class Solution{
        public boolean same(TreeNode p,TreeNode q){
            if(p==null && q==null) return true;
            if(p==null || q==null) return false;
            if(p.val!=q.val) return false;
            boolean left=same(p.left,q.left);
            boolean right=same(p.right,q.right);
            return left && right;
            
        }
    }
    public static void main(String[] args) {
        TreeNode p=new TreeNode(1);
        p.left=new TreeNode(2);
        p.right=new TreeNode(3);
        
        TreeNode q = new TreeNode(1);
        q.left=new TreeNode(2);
        q.right=new TreeNode(3);
        
        Solution sol = new Solution();
        System.out.println(sol.same(p,q));
    }
}