✅Reorder Linked List
⚡Approch:
1.Finding Middle Element,Reverse Second Half,Merge Two Half
2.Middle Element:
	Intialse slow and fast run a while using fast and check its next 
	elements should not be equals to null
	Move slow 1 step
	Move fast 2 step
	Slow points to middle
3.Reverse Second Half
	Create a reverselist using slow.next and create a private 	reverselist method later
4.Merge Two Half
	Use temp1 and temp2 and store first.next and second.next
	now first element from list1 to first element in list
	Also update temp1 and temp2 using first and second
5.Print The List
📌

class Main {
    static class ListNode{
        int val;
        ListNode next;
        ListNode(int val){
            this.val=val;
            this.next=null;
        }
    }
    static class Solution{
        public void reorderlist(ListNode head){
            //Middle
            //reverse secondhalf
            //merge
            ListNode slow=head;
            ListNode fast=head;
            while(fast.next!=null && fast.next.next!=null){
                slow=slow.next;
                fast=fast.next.next;
            }
            ListNode second=reverseList(slow.next);
            slow.next=null;
            
            ListNode first=head;
            while(second!=null){
                ListNode temp1=first.next;
                ListNode temp2=second.next;
                first.next=second;
                second.next=temp1;
                first=temp1;
                second=temp2;
            }
        }
        private ListNode reverseList(ListNode head){
            ListNode prev = null;
            ListNode curr = head;
            while(curr!=null){
                ListNode next = curr.next;
                curr.next=prev;
                prev=curr;
                curr=next;
            }
            return prev;
        }
    }
    
    public static void main(String[] args) {
        ListNode head = new ListNode(1);
        head.next=new ListNode(2);
        head.next.next=new ListNode(3);
        head.next.next.next=new ListNode(4);
        head.next.next.next.next=new ListNode(5);
        Solution sol = new Solution();
        sol.reorderlist(head);
        printList(head);
    }
    static void printList(ListNode head){
        while(head!=null){
            System.out.println(head.val);
            head=head.next;
        }
    }
}