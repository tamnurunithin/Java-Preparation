✅Merge Two Sorted Linked List
⚡Approch:
1.Three Building Blocks ListNode class,Solution class,main method,Print head
2.In ListNode class we just define val,next and constructor
3.Solution class has actual logic
	Intialise dummy node and also tail pointer
	Run a while loop where both list should not be null
	Compare Lists and add into dummy by using tail pointer
	Move current list value to next value
	If any values left in both list add them to dummy at the end
	Now return dummy
4.In main method
	Adding values to ListNode1
	Adding values to ListNode2
	Creating reference of solution class
	Print mergemethod by using reference 
5.Print has separate method that has parameters
	If head value is not equal to null
	print the head value
	move head to next
📌
class Main {
    static class ListNode{
        int val;
        ListNode next;
        ListNode(int val){
            this.val=val;
            this.next=null;
        }
    }
    static class Solution{
        public ListNode mergelistnode(ListNode list1,ListNode list2){
            ListNode dummy = new ListNode(-1);
            ListNode tail = dummy;
            while(list1!=null && list2!=null){
                if(list1.val<list2.val){
                    tail.next=list1;
                    list1=list1.next;
                }else{
                    tail.next=list2;
                    list2=list2.next;
                }
                tail=tail.next;
            }
            if(list1!=null){
                tail.next=list1;
            }else{
                tail.next=list2;
            }
            return dummy.next;
        }
    }
    public static void main(String[] args) {
        ListNode list1=new ListNode(1);
        list1.next=new ListNode(2);
        list1.next.next=new ListNode(3);
        
        ListNode list2=new ListNode(1);
        list2.next=new ListNode(4);
        list2.next.next=new ListNode(5);
        
        Solution sol = new Solution();
        ListNode merged=sol.mergelistnode(list1,list2);
        printList(merged);
        
    }
    static void printList(ListNode head){
        while(head!=null){
            System.out.println(head.val);
            head = head.next;
        }
    }
}

📌
#Leetcode ListNode is already defined
class Main{
public ListNode mergelistnode(ListNode list1,ListNode list2){
            ListNode dummy = new ListNode(-1);
            ListNode tail = dummy;
            while(list1!=null && list2!=null){
                if(list1.val<list2.val){
                    tail.next=list1;
                    list1=list1.next;
                }else{
                    tail.next=list2;
                    list2=list2.next;
                }
                tail=tail.next;
            }
            if(list1!=null){
                tail.next=list1;
            }else{
                tail.next=list2;
            }
            return dummy.next;
        }
    }
}