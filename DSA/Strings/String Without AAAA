public class Main {
    public static void main(String[] args) {
        String s = "AABBABA"; // input string
        System.out.println(checkString(s));
    }

    // Let's put the logic directly in main:
    public static void main(String[] args) {
        String s = "AABBABA";
        int countA = 0, countB = 0;
        boolean valid = true;

        for (int i = 0; i < s.length(); i++) {
            char c = s.charAt(i);
            if (c == 'A') {
                countA++;
                countB