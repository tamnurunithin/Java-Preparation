✅Minimum Absolute Difference SubArray
⚡Approach 
Check all possible pairs
Calculate absolute difference manually
Track the minimum difference and the pair
📌
class Main {
    public static void main(String[] args) {

        int[] arr = {3, 8, 15, 17};

        int minDiff = arr[1] - arr[0];
        if (minDiff < 0) {
            minDiff = -minDiff;
        }

        int a = arr[0];
        int b = arr[1];

        for (int i = 0; i < arr.length; i++) {
            for (int j = i + 1; j < arr.length; j++) {

                int diff = arr[i] - arr[j];
                if (diff < 0) {
                    diff = -diff;
                }

                if (diff < minDiff) {
                    minDiff = diff;
                    a = arr[i];
                    b = arr[j];
                }
            }
        }

        System.out.println("Pair: " + a + ", " + b);
        System.out.println("Minimum Absolute Difference: " + minDiff);
    }
}
